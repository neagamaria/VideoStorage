components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    loginUserDTO:
      properties:
        email:
          type: string
        password:
          type: string
    loginResponse:
      properties:
        token:
          type: string